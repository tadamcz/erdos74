# Provenance

`SHA256SUMS` lists the SHA-256 of each benchmark output file (the model's final `Spec.lean`, as verified by the benchmark's Comparator and released with the FrontierMath Erdős paper). Each `*.diff` is the unified diff from that file to the corresponding module in `Erdos74/Resolutions/`.
